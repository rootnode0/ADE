# SKILL: TDD Unit Test Writer
**Trigger:** task_type == "tdd-write"

## Rules
- **Location:** All tests must go in the `tests/` directory.
- **Naming:** Files must start with `test_`. Classes must inherit from `TestCase`.
- **Coverage:** Include 1 Happy Path (success) and 1 Edge Case (failure/validation error) for each function.
