[pytest]
python_files = tests/*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: mark test as requiring a live Ollama instance (deselect with -m "not integration")
addopts = -m "not integration"
